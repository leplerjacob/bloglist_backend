// const bcrypt = require('bcrypt')

// const createHash = (user) => {

//     return new Promise(async (resolve, reject) => {
//         const pw = user.passwordHash

//         const saltRounds = 10
//         await bcrypt.hash(pw, saltRounds, (err, hash) => {
//             if (err){
//                 reject(err)
//             } else {
//                 resolve(hash)
//             }
//         })
//     })
// }

// module.exports = createHash
